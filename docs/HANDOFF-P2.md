# Phase 2 인수인계

담당: Sol. 2026-09-23.

## DoD

- [x] 지도 클릭 좌표로 실제 현재·48시간·15일 예보를 갱신한다.
- [x] 화면에 실제 사용 소스와 발표/관측 시각을 표시한다.
- [x] KMA 장애 주입 시 Open-Meteo 폴백이 유지된다.
- [x] 동일 좌표 연속 호출이 `MISS → HIT`가 되고 적중률 헤더가 기록된다.
- [x] 소스 어댑터 커버리지: 라인 98.52%, 함수 100%.

현재 로컬에는 `KMA_SERVICE_KEY`가 없어 실시간 소스는 Open-Meteo와 Open-Meteo Marine이다. 키가 등록되면 국내 좌표의 현재값은 KMA 초단기실황을 우선하고 실패 시 자동 폴백한다. KMA 좌표 격자, 초단기실황, 초단기·단기예보 raw/normalize 경계를 구현했다.

검증: 단위 테스트 50개, 지도 클릭 실데이터 E2E 데스크톱·모바일, 프로덕션 빌드 41개 경로 통과. 실제 API 연속 호출에서 `OPEN_METEO`, `OPEN_METEO_MARINE`, 캐시 `MISS → HIT`, 적중률 `0.500`을 확인했다.
